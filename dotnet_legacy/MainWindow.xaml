<Window x:Class="KamisamaLoader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KamisamaLoader"
        xmlns:models="clr-namespace:KamisamaLoader.Core.Models;assembly=KamisamaLoader.Core"
        mc:Ignorable="d"
        Title="Kamisama Loader" Height="600" Width="800">
    <Grid>
        <TabControl>
            <!-- Browse Tab -->
            <TabItem Header="Browse">
                <Grid>
                    <ListView ItemsSource="{Binding GameBananaMods}" HorizontalContentAlignment="Stretch">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="5" Padding="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Image -->
                                        <Image Grid.Column="0" Source="{Binding PreviewImageUrl}" Height="80" Width="80" Stretch="UniformToFill" Margin="0,0,10,0"/>

                                        <!-- Details -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                            <TextBlock Text="{Binding Submitter.Name}" FontStyle="Italic"/>
                                            <TextBlock Text="{Binding RootCategory.Name}" Foreground="Gray"/>

                                            <!-- Download Button -->
                                            <Button Content="Download"
                                                    Click="DownloadButton_Click"
                                                    Tag="{Binding}"
                                                    HorizontalAlignment="Left"
                                                    Margin="0,5,0,0"
                                                    Padding="10,2"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </TabItem>

            <!-- Installed Mods Tab -->
            <TabItem Header="Mods Instalados">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListView x:Name="LocalModsList" ItemsSource="{Binding LocalMods}" HorizontalContentAlignment="Stretch" Grid.Row="0">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="LightGray" BorderThickness="1" Margin="2" Padding="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <CheckBox IsChecked="{Binding IsEnabled}" VerticalAlignment="Center" Margin="5"/>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0">
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"/>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="Ver: " Foreground="Gray" FontSize="12"/>
                                                <TextBlock Text="{Binding Version}" Foreground="Gray" FontSize="12" Margin="0,0,10,0"/>
                                                <TextBlock Text="By: " Foreground="Gray" FontSize="12"/>
                                                <TextBlock Text="{Binding Author}" Foreground="Gray" FontSize="12"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <Button Content="Update" Click="UpdateMod_Click" Tag="{Binding}" Margin="2" Background="Green" Foreground="White">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasUpdate}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                            <Button Content="Up" Click="MoveUp_Click" Tag="{Binding}" Margin="2"/>
                                            <Button Content="Down" Click="MoveDown_Click" Tag="{Binding}" Margin="2"/>
                                            <Button Content="Delete" Click="DeleteMod_Click" Tag="{Binding}" Margin="2" Background="Red" Foreground="White"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                         <Button Content="Check Updates" Click="CheckUpdates_Click" Margin="5" Padding="10,5"/>
                         <Button Content="Refresh" Click="RefreshLibrary_Click" Margin="5" Padding="10,5"/>
                         <Button Content="Build &amp; Install" Click="Build_Click" Margin="5" Padding="10,5" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Settings Tab -->
            <TabItem Header="Settings">
                <StackPanel Margin="20">
                    <TextBlock Text="Game Configuration" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <TextBlock Text="Game Executable Path:" Margin="0,5"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="GamePathTextBox" Text="{Binding GameExecutablePath, Mode=TwoWay}" Grid.Column="0" Margin="0,0,5,0"/>
                        <Button Content="Browse..." Click="BrowseGamePath_Click" Grid.Column="1"/>
                    </Grid>

                    <Button Content="Save Settings" Click="SaveSettings_Click" HorizontalAlignment="Left" Margin="0,20,0,0" Padding="10,5"/>
                    <Button Content="Register 1-Click Install" Click="RegisterProtocol_Click" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,5"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
