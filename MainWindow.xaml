<Window x:Class="KamisamaLoader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KamisamaLoader"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Kamisama Loader" Height="700" Width="1000">
    <Grid>
        <TabControl>
            <TabItem Header="Descobrir Mods">
                <Grid>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding GameBananaMods}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="LightGray" BorderThickness="1" Margin="5" Padding="5" Width="200" Height="250">
                                        <StackPanel>
                                            <Image Source="{Binding PreviewImageUrl}" Width="180" Height="120" Stretch="UniformToFill"/>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Submitter.Name, StringFormat='Por: {0}'}" FontStyle="Italic" TextWrapping="Wrap"/>
                                            <Button Content="Baixar e Instalar" Margin="0,10,0,0"/>
                                            <TextBlock Margin="0,5,0,0">
                                                <Hyperlink NavigateUri="{Binding ProfileUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                                                    Ver no GameBanana
                                                </Hyperlink>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Mods Instalados">
                <Grid>
                    <!-- Conteúdo para a aba "Mods Instalados" virá aqui -->
                    <TextBlock Text="Aqui você gerenciará seus mods instalados." HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>